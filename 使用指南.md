# 国赛 C 题数学建模辅助 Skill 使用指南

> 本指南说明本 Skill 的定位、交付边界和默认工作方式。使用前请先阅读。

## 一、定位

本 Skill 是 `math-contest-assistant` 的国赛 C 题优化分支，继承原有工具和复现能力，覆盖：

- 题目与附件审计；
- 模型设计与验证；
- Python/MATLAB 求解；
- 数据图、演示图与结果表；
- Word 论文生成；
- 按需生成 LaTeX/PDF；
- 复现、结构和内容一致性检查。

它支持六种工作模式：`data` 数据处理、`model` 建模、`code` 代码求解、`visualization` 可视化、`paper` 论文写作和 `full` 完整流程。严格按用户请求选择范围，不把单阶段任务自动扩成完整论文。

它用于形成可复核的研究草稿和交付包，不替代参赛队伍的判断、核对和最终改写。

## 二、交付边界

生成的分析、代码、结果、图表和论文都必须经过人工核对后才能用于正式提交。

| 产物 | 用途 | 提交前必须核对 |
|---|---|---|
| `题目分析报告.md`、`术语表格.md` | 建模草稿 | 子问题、假设、公式、单位和约束 |
| `.work/model-contract.json` | 机器可读模型合同 | 主模型、参数、验证指标和失败条件 |
| Python/MATLAB 代码 | 求解草稿 | 真实运行、边界、单位、随机种子和可解释性 |
| `results/`、`figures/` | 结果与证据 | 数值、图表来源、题注和正文引用 |
| `完整论文.docx` | 默认论文草稿 | 规则、格式、公式、引用、页数和人工改写 |
| LaTeX 源码/PDF | 用户显式要求时的论文草稿 | 官方模板、真实编译、日志、字体、页数和资源哈希 |

本 Skill 不保证模型、代码、引用或论文一定正确，也不保证竞赛成绩。任何关键数值、公式、图表和引用都应由队伍独立复核。

## 三、默认格式

- 未指定格式时，默认生成 `完整论文.docx`。
- 用户明确要求 LaTeX、PDF 或双格式时，才生成 `完整论文-LaTeX/`、`完整论文.pdf`，或同时生成 Word 与 LaTeX。
- Word 与 LaTeX 同时生成时，必须共用同一份模型合同、结果、图表、引用和 Claim–Evidence Matrix。
- 当届竞赛官方规则和模板优先于本 Skill 的默认值。

## 四、阶段与门禁

完整流程为：

1. 输入与数据：附件审计、数据预处理、探索和 `D1`；
2. 分析与基线：拆题、题型识别、简单基线和 `B1`；
3. 建模：候选比较、复杂度审计、模型合同和 `M1`；
4. 编程：最小闭环 `P1`、全量求解、复现和 `P2`；
5. 论文：Claim–Evidence Matrix 与 `W1`，写作、排版和 `W2`。

单阶段请求只执行所需阶段；缺少必要上游证据时只补齐最小依赖。只写论文必须核对真实结果，只写代码必须有模型合同，只做图表必须使用可定位的真实数据。

## 五、可选协作

默认只运行固定质检门禁。以下协作全部默认关闭，只有用户明确启用时才运行：

- 当届官方规则与模板核验；
- 附件字段、行数、缺失和异常盘点；
- 文献与模型族调研；
- 隔离算法原型；
- 独立实验批次；
- Python/MATLAB 对照实现；
- 术语与英文表达核验。

协作者只返回证据、实验结果或建议，不直接修改权威模型、代码、结果和论文。主 Agent 负责取舍、写入和复验。

## 六、提交前人工核对

- [ ] 所有子问题均已回答，且结论来自真实运行；
- [ ] 公式、代码、表格、图表和单位保持一致；
- [ ] 每个摘要和结论主张都能在 Claim–Evidence Matrix 中指向公式、表、图、代码或验证记录；
- [ ] 引用可追溯到原始论文、标准、官方规则或官方数据；
- [ ] 论文结构和格式符合目标竞赛当届规则；
- [ ] 队伍成员已阅读、改写并确认最终稿，不原样提交 Skill 生成的内容。
