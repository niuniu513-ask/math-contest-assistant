---
name: math-contest-assistant
description: 自主完成数学建模竞赛任务的全流程技能。用于 CUMCM、MCM/ICM 及其他竞赛的赛题解析、附件审计、模型设计、代码求解、统计与数值验证、可视化、论文写作、Word/LaTeX 排版和复现交付；也用于只做建模分析、代码、图表或论文的单阶段任务。用户提到数学建模、数模竞赛、国赛、美赛、CUMCM、MCM/ICM、建模论文或多问式建模题时使用。
---

# 数学建模竞赛自主解题

目标是在信息足够时持续推进到可复现交付，而不是停留在方法建议。事实、计算结果、引用和格式要求必须可追溯；生成内容仅供用户参考，并须遵守目标竞赛当届规则。

## 1. 首次动作

1. 确定 `SKILL_ROOT`（本文件目录）和 `PROJECT_ROOT`（用户项目目录）。输入保持只读，产物只写入 `PROJECT_ROOT`。
2. 枚举题目、附件、模板和既有产物；识别竞赛、年份、题号、语言、交付格式和剩余时间。不能确认时记录假设，不因低风险信息缺失而停工。
3. 根据用户意图选择完整流程或单阶段流程。不要把单阶段请求扩成完整论文。
4. 读取 `references/自主解题协议.md`，初始化或恢复 `.work/run-state.json`：

```powershell
python "<SKILL_ROOT>/scripts/project_state.py" init --project-root "<PROJECT_ROOT>" --mode full
python "<SKILL_ROOT>/scripts/project_state.py" status --project-root "<PROJECT_ROOT>"
```

5. 首次进度更新只报告：识别结果、当前状态、下一项可验证动作、阻塞或关键假设。

## 2. 自治原则

- 默认持续执行，但 `M1` 建模门禁后必须按“建模确认点”暂停；此外仅在缺少不可替代输入、需要用户作会实质改变目标的选择、需要外部授权，或即将执行高风险操作时暂停。
- 建模阶段自行完成候选比较和推荐，不把所有细节问题抛给用户；提交完整推荐方案后集中请求一次确认。
- 先做最小闭环，再扩大计算。优先跑通“一个真实输入 → 一个可信结果 → 一个验证”，然后全量运行。
- 不把往届论文的写作习惯当作官方规则。经验统计只能作为软建议，详见 `references/语料证据使用指南.md`。
- 不为显得复杂而堆叠模型。每个方法都必须承担明确功能，并有相应数据、假设和验证。
- 不伪造结果、引用、工具输出或团队讨论。不得人工编写本应由代码产生的指标。
- 若中途失败，保存状态、错误证据和下一步；从最近有效检查点恢复，不从头重复。

## 3. 状态机与门禁

完整任务依次执行下列状态。单阶段任务从所需状态进入，并补齐其不可缺少的上游证据。

| 状态 | 核心动作 | 最小退出条件 |
|---|---|---|
| `intake` | 盘点题目、附件、模板、规则 | 输入清单完整；缺失项已分类 |
| `parse` | 逐问拆解目标、变量、约束、单位、输出模板 | 每个子问题都有可检验的输出定义 |
| `research` | 检索相似赛题、理论依据和方法候选 | 来源与当前题的可迁移部分已说明 |
| `model` | 建立模型合同、候选比较、验证与回退条件 | `M1` 通过；向用户汇报方案并取得确认 |
| `prototype` | 真实数据纵向切片或结构等价小实例 | `P1` 通过；核心链路可运行 |
| `solve` | 全量求解、稳健性/不确定性分析、题目指定表格 | 结果完整且约束可行 |
| `validate` | 独立重算、边界/量纲/统计/数值检查 | `P2` 通过；复现清单有效 |
| `evidence` | 建立主张—公式—表—图—代码映射 | `W1` 通过；无悬空结论 |
| `write` | 按证据写作并使用官方模板排版 | 内容与真实结果一致 |
| `package` | 编译、渲染、格式校验、一致性检查 | `W2` 通过；交付包可复现 |

门禁结果使用 `PASS`、`FAIL`、`BLOCKED`。环境支持独立质检 Agent 时按 `references/Subagent调度.md` 派发；否则按对应清单逐项自审并标记 `LIMITED`，不得伪称独立验收。

任何门禁后相关产物发生实质变化，原门禁失效。用 `project_state.py advance` 记录状态、证据和门禁结果。

### 建模确认点

完成 `M1` 后必须暂停，向用户提交逐问模型、关键假设、变量与约束、算法路线、必要演示/实例、验证方法、主要风险和备选方案，并明确询问是否按此方案进入编程。用户确认前不得推进到 `prototype`、运行正式求解或撰写结果；用户要求修改时留在 `model` 阶段更新合同并重新执行 `M1`。仅当用户在当前请求中明确授权“无需确认、直接完成”时可跳过。推进时必须向 `project_state.py advance` 传入 `--user-approval`，把确认或预先授权写入状态文件。

## 4. 分阶段加载

只读取当前动作需要的入口，禁止一次性加载全部资料。

| 当前动作 | 必读入口 | 按需入口 |
|---|---|---|
| 跨技能协同 | `references/技能协同协议.md` | 当前阶段所需的外部 Skill |
| 题目与附件解析 | `references/自主解题协议.md` | `tools/pdf/SKILL.md`、`tools/xlsx/SKILL.md` |
| 建模与选型 | `references/建模工作流程.md`、`references/建模质检清单.md` | `references/算法索引.md`、相关 `assets/*.md`、`assets/08-晴天资料方法卡.md`、`references/model-catalog-guide.md` |
| 使用往届资料 | `references/语料证据使用指南.md` | `references/获奖论文实读方法论.md`、`references/contest-questions-database.md`、`references/award-paper-patterns*.md` |
| 编程与复现 | `references/编程工作流程.md`、`references/编程质检清单.md`、`scientific-toolkit-skill` | `references/code-generation-guide.md`、`references/MATLAB规范.md`、`tools/xlsx/SKILL.md` |
| 图表 | `references/可视化规范.md` | `references/图表选择与避坑.md` |
| 论文内容 | `references/论文写作工作流.md`、`references/深度写作与竞争力门禁.md`、`references/论文自审框架.md`、`research-writing-skill` | `humanizer_academic`（事实冻结后的终稿润色） |
| Word/演示排版 | `office-academic-skill` | `tools/docx/SKILL.md`、`tools/latex/SKILL.md`、格式规范 |
| 联网取证 | `web-access` | 官方站点、原始论文页、数据发布页 |
| 文献搜索 | `tools/paper_search/SKILL.md` | 原始出版页或 DOI |

## 5. 题目解析与输入审计

1. 先读题目正文，再枚举全部附件；不能只根据标题或题型字母选模型。
2. 为每个子问题记录：目标、输入、决策变量、约束、评价指标、单位、输出文件/模板和与前问依赖。
3. 对 PDF 使用能力探测链：文本提取 → 编码修复 → 页面渲染/OCR → 关键页视觉核对；用页数、标题、问题编号和附件引用检查完整性。
4. 对表格保留原文件，核对工作表、表头、首尾行、行列数、公式、缺失、重复、异常、时间顺序和单位。禁止把大附件硬编码进源代码。
5. 题目要求填写结果模板时，先复制模板，保留工作表名、单元格结构和格式，再写入计算结果。

## 6. 建模要求

1. 先写模型合同，再写代码。合同包括变量、参数、目标/方程、约束、单位、输入输出、假设、验证指标和失败条件。
2. 每个子问题比较 1–3 个真正可行的模型族；按适配性、可辨识性、数据需求、计算代价、解释性和验证难度选择。简单模型足够时优先简单模型。
3. 机理题优先守恒律、几何与边界条件；数据题先划分训练/验证信息边界；优化题先验证可行域和基线解；仿真题先验证事件逻辑与极端情形。
4. 把相似历年题用于发现结构和风险，不复制其数值、结论或模型组合。内部研究记录应标明样本年份、题号、页面和可迁移依据；正式竞赛稿禁止出现同届/未来获奖论文、内部样本编号、对标路径或赛后答案。
5. 输出 `题目分析报告.md`、`术语表格.md` 和机器可读的 `.work/model-contract.json`。

## 7. 编程、求解与验证

1. 根据模型、现有资产和运行环境选择 Python 或 MATLAB；二者都是一等实现路线。按数据读取、清洗、模型、验证、图表和导出分层实现；路径相对 `PROJECT_ROOT`，参数集中配置，随机过程固定种子。
2. 先运行最小实例并记录命令、退出码和结果摘要；通过 `P1` 后才做全量计算。
3. 验证方式由模型决定，不机械全做：
   - 回归/分类：适当的数据切分、基线、交叉验证、校准或残差诊断；
   - 时间序列：防止未来信息泄漏，使用滚动/时序验证；
   - 优化：检查全部约束、最优性界或与基线/替代求解器比较；
   - 数值模型：步长/网格收敛、守恒、边界和极限情形；
   - 随机仿真：重复运行、置信区间和种子稳定性。
4. 灵敏度范围来自物理范围、题设区间、估计不确定性或文献，不默认使用 `±20%`。
5. 数值保留位数由测量精度、误差和决策需求决定；禁止用无意义的小数位制造精确感。
6. 生成 `results/复现清单.json`：

```powershell
python "<SKILL_ROOT>/scripts/repro_manifest.py" --project-root "<PROJECT_ROOT>" --input "<INPUT>" --seed 42 --parameters '{}' --command "<唯一复现命令>"
```

## 8. 可视化与结果表

- 每张图必须支撑一个明确结论，并映射到子问题和结果数据。图的数量、类别和版面由证据需求决定，不设固定九图门槛。
- 每个核心模型至少提供一幅必要的演示图或实例图，用小型算例、几何关系、状态演化、网络局部结构、信号处理前后或算法迭代过程解释模型如何工作；另用结果图或结果表回答题目。演示图不能用无数据含义的装饰图代替。
- 优先导出可编辑 SVG/PDF 和 ≥300 DPI PNG；目标模板不支持时使用其允许格式。
- 使用 `figure_audit.py` 检查已有图；只有项目质量计划明确要求三类图时才启用类别门槛：

```powershell
python "<SKILL_ROOT>/scripts/figure_audit.py" "<PROJECT_ROOT>/figures" --no-category-check --strict
```

- 图表创建后必须实际查看最终尺寸的彩色和灰度版本，检查缺字、裁切、重叠、误导尺度和单位。
- 结果表保留足够精度用于复算；论文展示值可以合理舍入，但必须能追溯到权威结果文件。

## 9. 论文与格式

1. 先建立 Claim–Evidence Matrix；每个关键结论绑定代码输出、表或图。证据不足时回退求解，不得先写结论再补数字。
2. 沿用 `references/论文写作工作流.md` 和 `references/深度写作与竞争力门禁.md`。写作前生成 `.work/derivation-ledger.json`、`.work/decision-traces.json` 和 `.work/benchmark-gap.json`；缺少推导台账不得进入长篇正文。
3. 论文篇幅由题目复杂度、证据完整性、官方规则和用户要求共同决定，不设默认最低页数；实际渲染后记录主体、附件和总页数。不得用重复文字、空白、放大字号或拉伸图表凑页数。
4. 主体篇幅由完整的问题分析、假设依据、逐步模型推导、算法分步说明、数值代入实例、演示/实例图、结果、独立验证、敏感性或不确定性分析、评价和局限共同支撑。每一处近似写明成立条件并估计误差量级；无法估计时降低结论强度。
5. 优化模型完整列出目标和全部约束并逐项解释；统计/机器学习模型完整说明预处理、特征选择、参数确定、验证设计和指标理由。后问开头说明与前问共享的具体变量、公式、字段和误差传播。
6. 每张正式图和核心结果表必须有相邻的“趋势/特征 → 成因 → 对模型或决策的启示”解读，包含至少一个可读数值。禁止“该图用于说明结果”式空话。
7. 公式源遵守 `references/深度写作与竞争力门禁.md`：行内 `$...$`，独立 `\[...\]` 或标准环境，禁止 `$$...$$`；需要引用的独立公式全局连续编号，变量、向量/矩阵、导数和单位语义一致。
8. 初稿后执行最多三轮“自我审视—扩充”，只补真实推导、示例、对比和验证；发现证据缺失时回退求解，不以文字扩充代替实验。
9. 默认只生成用户要求的格式；用户要求完整交付但未指定时，优先生成可可靠验证的一种格式，再视环境生成另一种。
10. Word 使用 `tools/docx/SKILL.md`；LaTeX/PDF 使用 `tools/latex/SKILL.md`。官方当届模板优先于内置模板和历史模板。
11. 引用必须可追溯。获奖论文只能用于内部方法与表达研究，正式竞赛稿不得引用同届/未来获奖论文或暴露内部对标编号；理论依据回到原始论文、标准、教材和官方来源。
12. 写作完成后先运行 `paper_content_audit.py`，再核对数值、单位、符号、图表编号、引用、附件文件、摘要和正文页数；语言润色不得改变技术事实。

```powershell
python "<SKILL_ROOT>/scripts/paper_content_audit.py" "<PROJECT_ROOT>/论文正文.md" `
  --project-root "<PROJECT_ROOT>" `
  --derivation-ledger "<PROJECT_ROOT>/.work/derivation-ledger.json" `
  --decision-log "<PROJECT_ROOT>/.work/decision-traces.json" `
  --benchmark-gap "<PROJECT_ROOT>/.work/benchmark-gap.json" `
  --final-docx "<PROJECT_ROOT>/完整论文.docx" `
  --strict
```

## 10. 阻塞与回退

| 失败类型 | 处理 |
|---|---|
| 题目/附件缺页 | 用目录、问题编号和引用核验；请求缺失文件，同时推进可独立部分 |
| PDF 无文本或乱码 | 渲染/OCR并抽查关键页；记录识别置信度 |
| 模型不可辨识/不可行 | 简化模型、补充约束或返回 `model`，保留失败证据 |
| 依赖缺失 | 报告精确依赖；仅在数学等价且验证不降级时换实现 |
| 求解超时 | 保存最好可行解和界，缩小实例定位瓶颈，再换算法或分解 |
| 结果不稳定 | 扩大重复、检查尺度/泄漏/病态性，降低结论强度 |
| 排版工具缺失 | 交付已验证源码/中间格式并标记 `BLOCKED`，不伪造 PDF/DOCX |

## 11. 完成判定

只有同时满足以下条件才可声明完成：

- 题目全部子问题和题目指定附件均已交付；
- 所有核心数值来自实际运行，约束、单位、边界和不确定性已检查；
- 唯一复现命令可从 `PROJECT_ROOT` 运行，输入哈希与依赖已记录；
- 论文主张与公式、表、图、代码结果一致，引用可追溯；
- 论文篇幅符合当届规则和用户要求，主体与附件口径已记录；
- 每个核心模型均有必要的演示图/实例图和对应的结果证据；
- 完整竞赛论文达到深度质量档：≥15 个连续编号有效公式、≥12 幅非装饰图、≥8 条真实领域文献，并通过双方法验证、三重敏感性、物理目标对齐和工程裕度检查；
- 最终正文、参考文献和附录不含任何已知获奖论文信息、内部对标内容、纯文本数学表达式或模板化图表解读；
- 3–5 处团队/方法选择痕迹均来自真实日志和实验；无法满足时阻断而非编造；
- 目标格式已真实编译/生成并完成结构与视觉检查；
- 相关门禁为 `PASS`，或在无独立质检环境下明确标记 `LIMITED`；
- 最终回复列出交付物、关键命令及退出码、门禁状态、核心指标和仍存在的限制。
