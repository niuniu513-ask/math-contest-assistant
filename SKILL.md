---
name: math-contest-assistant-c
description: 全国大学生数学建模竞赛 C 题优化版辅助技能，继承 math-contest-assistant 的附件审计、建模、编程、验证、图表、论文、Word/LaTeX 与复现能力，并强化数据预处理前置、简单基线和模型复杂度控制。可单独完成数据处理、建模、代码与可视化、论文写作，也可运行完整流程。用户提到国赛 C 题、CUMCM C 题、数据驱动型国赛题或要求完成 C 题建模论文时使用。
---

# 国赛 C 题全流程建模

本 Skill 是 `math-contest-assistant` 的 C 题优化分支。沿用原 Skill 的工具、脚本和质量门禁；新增内容不得削弱真实性、可复现性和官方格式要求。目标是在竞赛时间内，以能够充分回答题目的最低必要复杂度，形成数据、模型、结果和论文闭环。

## 1. 首次动作

1. 确定 `SKILL_ROOT` 和用户项目 `PROJECT_ROOT`。输入保持只读，产物只写入项目目录。
2. 读取 `使用指南.md`、`references/cumcm-c/C题自主解题协议.md`，盘点题面、附件、模板和已有产物。
3. 初始化或恢复状态：

```powershell
python "<SKILL_ROOT>/scripts/project_state.py" init --project-root "<PROJECT_ROOT>" --mode full
python "<SKILL_ROOT>/scripts/project_state.py" status --project-root "<PROJECT_ROOT>"
```

4. 首次更新只报告识别结果、当前阶段、下一项可验证动作和关键假设。
5. 完整流程在 M1 建模确认点暂停；用户已明确授权直接完成时可记录预先授权后继续。

## 2. 状态机

| 状态 | 核心动作 | 退出条件 |
|---|---|---|
| `intake` | 盘点题目、附件、模板和规则 | 输入清单完整 |
| `data` | 数据审计、预处理、探索和可辨识性分析 | D1 通过 |
| `parse` | 逐问拆解目标、输出、约束、单位和依赖 | 输出定义可检验 |
| `research` | 查找理论依据与候选方法 | 来源可追溯 |
| `baseline` | 按题型运行最低复杂度基线 | B1 通过 |
| `model` | 比较候选、控制复杂度、冻结模型合同 | M1 通过并确认 |
| `prototype` | 真实切片或结构等价小实例 | P1 通过 |
| `solve` | 全量求解和必要实验 | 结果完整可行 |
| `validate` | 独立复算、样本外或针对性验证 | P2 通过 |
| `evidence` | 建立主张—证据矩阵和推导台账 | W1 通过 |
| `write` | 按证据完成论文正文 | 内容审计通过 |
| `format` | 官方模板排版、逐页视觉检查 | W2 通过 |
| `package` | 交付论文、代码、结果和复现材料 | 交付一致 |

门禁状态使用 `PASS`、`FAIL`、`BLOCKED` 或无独立质检条件下的 `LIMITED`。上游产物实质变化后，相关门禁自动失效。

## 3. 数据必须前置

只允许先读题面以理解附件语义；正式主模型不得在 D1 前冻结。读取 `references/cumcm-c/数据预处理与D1门禁.md`，并至少回答：

- 数据结构、字段、单位、样本和时空范围是什么；
- 缺失、重复、异常、冲突、格式错误和时间泄漏是否存在；
- 哪些处理会改变建模结论；
- 数据量与信息量是否支持候选模型复杂度；
- 哪些变量可估计，哪些只能由题设、文献或假设给出；
- 哪些数据特征支持模型选择；
- 处理前后是否可追溯。

产出 `data/数据质量报告.json`、`data/处理决策日志.json`、`data/处理前后统计对照.csv`、`.work/data-contract.json` 和 `数据预处理与探索分析.md`。无结构化数据时改做题设数值、量纲、边界、初值和参数可辨识性审计，不得跳过 D1。

## 4. 基线与模型复杂度

读取 `references/cumcm-c/模型复杂度与基线.md`。每个子问题先运行简单基线并保存 `.work/baseline-registry.json` 与 `results/baselines/`。基线不一定进入正文，但必须进入证据与复现清单。

复杂度按 L0–L4 逐级控制：

| 等级 | 方法范围 | 使用条件 |
|---|---|---|
| L0 | 描述统计、直接计算、规则法 | 所有任务起点 |
| L1 | 线性模型、基础回归、插值、确定性规划 | 能直接表达题意时优先 |
| L2 | 非线性、多目标、时间序列、标准机器学习、蒙特卡洛 | L1 存在可验证缺陷 |
| L3 | 随机/鲁棒优化、复杂仿真、集成学习、启发式算法 | L2 不能满足关键要求 |
| L4 | 深度学习、复杂贝叶斯、混合元启发式、多模型集成 | 数据规模、题意和实验收益均支持 |

升级必须回答：简单模型失败在哪里；新增了什么变量、假设和计算成本；用什么预先确定的指标证明升级有效；改善是否覆盖复杂度与时间成本；团队能否解释和复现。任何一项回答不清楚，不得升级。原则上不得跳级；题目结构明确要求时可跳级，但必须说明中间层为什么不适用。

候选比较记录到 `.work/model-complexity.json`。复杂模型未修复基线关键缺陷、只改善非关键指标或收益不足以覆盖成本时，回退简单模型。禁止为了名称、篇幅、公式数量或模仿获奖论文堆叠模型。

## 5. 任务模式

根据用户当前请求选择最小充分流程，不把单阶段请求擅自扩成完整论文：

| 模式 | 主要工作 | 必要上游证据 | 默认交付 |
|---|---|---|---|
| `data` | 附件审计、清洗、探索、特征和数据报告 | 题面与原始附件 | 处理数据、D1 报告、图表与复现脚本 |
| `model` | 问题分析、基线、候选比较和模型合同 | D1 或等价输入/条件审计 | 分析报告、B1、模型合同、M1 建议 |
| `code` | 实现主模型、求解、验证和结果导出 | 已确认模型合同；缺失时只补最小合同 | 源码、权威结果、日志、复现清单 |
| `visualization` | 基于真实数据或权威结果制作图表 | 可定位的数据与结果字段 | 图表、源数据、生成脚本、清单 |
| `paper` | 依据真实模型、代码和结果写作排版 | 模型合同、权威结果、验证和证据矩阵 | Word；按需 LaTeX/PDF |
| `full` | 从输入审计持续推进到完整交付 | 题面、附件和必要模板 | 全部阶段产物 |

单阶段仍须补齐不可缺少的最小依赖。例如只写论文时先核对真实结果和公式—代码一致性；只写代码时不得用占位模型代替缺失合同；只做可视化时不得编造数据。完成请求范围后停止，不继续执行未授权阶段。

初始化示例：

```powershell
python "<SKILL_ROOT>/scripts/project_state.py" init --project-root "<PROJECT_ROOT>" --mode data --stage data
python "<SKILL_ROOT>/scripts/project_state.py" init --project-root "<PROJECT_ROOT>" --mode visualization --stage solve
```

## 6. 编程、求解与验证

沿用 `references/编程工作流程.md`、`references/编程质检清单.md` 和原有 Python/MATLAB 规范。主模型代码必须来自 `.work/model-contract.json`，先最小闭环再全量计算，固定随机种子并生成 `results/复现清单.json`。

验证随题型选择，不机械堆叠：预测与分类做合适的时序/样本外验证；评价模型做权重与排序稳定性；优化检查约束、界和基线；仿真检查极端情形与重复稳定性；机理模型检查量纲、守恒和收敛。主模型必须与同口径基线比较。

## 7. 论文内容

读取 `references/cumcm-c/C题论文结构与公式.md`、原有论文写作工作流和 Claim–Evidence 规范。

- 正文内部质量目标为 25–30 页，并始终服从当届官方上限；摘要原则上一页，正文不以目录、重复文字、空白、字号或代码截图凑页。
- 完整论文通常有 20–35 个有效编号公式。数量用于发现推导缺口，不单独构成通过条件。
- 核心推导覆盖变量与参数、原始关系、假设与简化、目标/方程、约束、变换或线性化、参数获得、求解和验证指标。
- 拆分简单定义、重复总模型、未用于求解或与代码不一致的公式不计数。
- 完整论文通常有 12–18 幅有效图和 8–12 张核心表，覆盖数据理解、模型机制、核心结果、方案比较和验证；数量不适合题目时按证据需要调整并说明。
- 图表必须绑定真实来源、主张和关键数值；流程图、装饰图和重复信息不能替代结果证据。
- 图表分析自然写入论证，禁止批量使用“对图 X 的解读如下”等固定句式。
- 篇幅不足时依次补数据洞察、真实推导、数值算例、候选比较、样本外验证、误差分析和决策解释。

## 8. 格式与排版

官方当届模板和规则优先。读取 `references/cumcm-c/C题Word排版规范.md`，并沿用 `tools/docx/SKILL.md`：

- 标题、正文、公式、图题、表题、页码、参考文献和超链接显示文字统一黑色；
- 中文正文用宋体，英文、数字和公式优先 Times New Roman，各级标题用黑体；
- 彩色仅用于确有信息区分需要的图表，并保证灰度可辨；
- 图题在图下、表题在表上，表格优先三线表，公式居中且编号右对齐；
- Word 与 PDF 的内容、分页、编号、引用一致；逐页检查截断、孤行、大片空白、表格断裂、颜色和字体。

使用 `scripts/docx_style_audit.py` 检查最终 Word 的非黑色文字和字体槽设置。

## 9. 完成判定

只有同时满足以下条件才可声明完成：

- 全部子问题、附件和题目指定模板均已交付；
- D1、B1、M1、P1、P2、W1、W2 已通过或如实标记限制；
- 每问都有实际运行的简单基线，主模型复杂度有证据支持；
- 核心结果来自真实运行，约束、单位、边界和不确定性已检查；
- 公式、图表、表格、代码和论文结论一致且可追溯；
- 正文达到 25–30 页内部目标或记录合理例外，并符合官方上限；
- 正文文字样式全黑，最终格式完成结构与逐页视觉检查；
- 唯一复现命令有效，输入哈希、依赖、参数和随机种子已记录；
- 最终回复列出交付物、复现命令、门禁状态、核心指标、运行时间和限制。
